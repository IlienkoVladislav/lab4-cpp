#include "asyncFileOperations.h"
#include <windows.h>
#include <iostream>
#include <vector>

void processFilesAsync(const std::vector<std::wstring>& inputFiles, const std::wstring& outputFile) {
    std::wcout << L"Async file processing is not yet implemented." << std::endl;
}
